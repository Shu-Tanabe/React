import React from 'react';
import Menu from './Menu';


const RegisterData: React.FC = () => {
    return (
        <Menu>
            <h1>add</h1>
        </Menu>
    );
};

export default RegisterData;